import { Routes } from '@angular/router';
import { HomePageComponent } from './home-page';

export const routes: Routes = [
	{
		path: '',
		component: HomePageComponent,
		title: 'Javier Lerones | Portfolio'
	}
];
